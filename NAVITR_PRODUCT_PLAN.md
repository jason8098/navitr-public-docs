# 나비트레이딩 제품 기획서

> 문서 버전: 1.0  
> 작성 기준일: 2026-07-30  
> 제품 단계: 실거래 연동 프로토타입 / 안정화 단계  
> 문서 목적: 현재 제품 범위, 운영 결과, 리스크, 개선 계획, 로컬 라이선스 제품 및 API 검증 앱의 개발 방향 합의

---

## 들어가며

나비트레이딩은 단순히 급등 종목을 보여주는 시세 화면에서 출발한 프로젝트가 아니다. 장중에 수십 개 종목의 가격과 체결 흐름을 사람이 계속 지켜보는 부담을 줄이고, 사용자가 미리 정의한 기준에 따라 신호 확인부터 주문, 체결 추적, 청산까지 이어지는 과정을 하나의 시스템 안에서 처리하는 것이 이 프로젝트의 출발점이다.

실제 단타 매매에서는 가격이 올랐다는 사실만으로는 진입 여부를 판단하기 어렵다. 짧은 시간에 얼마의 자금이 들어왔는지, 거래량이 평소보다 얼마나 증가했는지, 매수 체결이 우세한지, 이미 상승이 끝난 뒤 뒤늦게 감지된 것은 아닌지까지 함께 봐야 한다. 주문을 낸 뒤에도 문제가 끝나는 것은 아니다. 일부 수량만 체결되거나, 지정가가 시장에서 멀어져 미체결로 남거나, 정정·취소 요청과 실제 체결이 엇갈리는 상황을 계속 추적해야 한다.

현재 나비트레이딩은 이 전체 흐름을 실제 증권사 API와 연결한 프로토타입까지 구현한 상태다. 급등 감지, 사용자 승인 주문, 완전 자동 주문, 잔고와 체결 조회, 익절·손절·고점 추적 등 주요 기능은 이미 존재한다. 동시에 실거래 테스트를 통해 증권사 API 한도, 실시간 구독 누락, 주문 상태 동기화, 손익 계산 기준처럼 상용화 전에 반드시 해결해야 할 문제도 구체적으로 드러났다.

따라서 이 문서는 기능을 나열하는 데 목적이 있지 않다. 현재 제품이 어디까지 구현됐고 무엇이 아직 위험한지, 운영 과정에서 어떤 문제가 확인됐으며 어떤 순서로 개선해야 하는지, 이후 로컬 설치형 제품과 별도 API 검증 도구로 어떻게 확장할지를 하나의 제품 계획으로 정리한다.

---

## 1. 경영진 요약

나비트레이딩은 국내 주식의 실시간 체결 데이터를 감시하여 짧은 시간에 가격과 수급이 급변하는 종목을 탐지하고, 사용자가 설정한 조건에 따라 매수 확인 또는 자동 주문을 수행하는 단타 매매 지원 시스템이다.

사용자는 감시할 시장과 종목 범위를 지정한 뒤 상승률, 거래대금, 거래량, 체결강도와 같은 기준을 전략으로 저장할 수 있다. 시장에서 조건을 만족하는 움직임이 발생하면 시스템은 신호를 만들고, 전략 실행 방식에 따라 사용자에게 승인을 요청하거나 자동으로 주문을 전송한다. 매수가 체결된 뒤에는 익절, 손절, 시간 청산, 고점 추적 조건에 따라 매도 시점을 관리한다.

현재 제품은 다음 기능을 실제 코드로 보유하고 있다.

- 한국투자증권 계좌 연결 및 모의·실전 계좌 분리
- 실시간 시세, 시장 중심 종목, 관심종목, 뉴스, 차트 제공
- 가격 상승률, 체결 건수, 거래대금, 거래량, 총 거래대금, 체결강도 등 복합 조건을 이용한 급등 감지
- 사용자 확인 주문과 완전 자동 주문
- 지정가, 시장가, 지정가 후 시장가 주문
- 익절, 손절, 시간 청산, 고점 추적 청산
- 미체결 주문 조회, 정정, 취소 및 체결 이력
- 잔고, 보유 종목, 평가손익, 전략별 거래 분석
- 계좌·전략·신호·주문·포지션의 사용자별 분리
- MySQL 영속 저장, Redis 실시간 캐시·이벤트, WebSocket 화면 갱신

이 정도의 기능 범위만 보면 자동매매 제품의 기본 골격은 갖춰진 것으로 볼 수 있다. 그러나 기능이 존재하는 것과 실전에서 안전하게 동작하는 것은 별개의 문제다. 현재 상태는 **실제 자금을 장시간 무인 운용할 수 있는 상용 완성 단계가 아니다.** 실거래 테스트 과정에서 증권사 API 구독 한도, 주문 상태 지연, 부분 체결, 미체결 정정·취소, WebSocket 재연결, 손익 계산 기준 차이 등 중요한 문제가 반복 확인됐다.

특히 자동매매 시스템에서 가장 중요한 기준은 화면이 빠르게 보이는지가 아니라 증권사 원장과 내부 상태가 항상 일치하는지다. 주문이 체결됐는데 미체결로 남아 있거나, 취소된 주문을 계속 추적하거나, 보유 수량과 매도 가능 수량을 잘못 인식하면 손실 규모가 커질 수 있다. 현재 프로젝트의 다음 단계는 새로운 전략을 많이 추가하는 것이 아니라 이러한 상태 정합성을 제품 수준으로 끌어올리는 것이어야 한다.

따라서 단기 목표는 신규 전략 추가보다 다음 세 가지다.

1. 주문·체결·잔고를 증권사 원장과 일치시키는 정합성 확보
2. 모든 급등 판단 근거를 재현할 수 있는 틱 기록·리플레이 체계 구축
3. 실주문 전 Shadow Mode와 모의투자로 전략 성능을 검증하는 절차 확립

중장기적으로는 중앙 웹 서비스뿐 아니라 사용자 PC에서 백그라운드로 실행되는 로컬 라이선스 제품을 제공한다. 로컬 제품은 증권사 인증정보를 사용자 PC 밖으로 보내지 않고, 중앙 서버는 라이선스 발급·업데이트·선택적 원격 진단만 담당하는 구조를 목표로 한다. 사용자가 화면을 닫더라도 로컬 서비스가 주문과 보유 포지션을 계속 관리하도록 구성하면 중앙 서버 장애의 영향도 줄일 수 있다.

별도로 **API 급등 감지 검증 앱**을 개발한다. 이 앱은 증권사 API의 시세·체결·잔고·주문·정정·취소·WebSocket·호출 제한을 한 화면에서 테스트하고, 모든 전략 파라미터가 어떤 데이터로 통과·실패했는지 확인하는 내부 품질 도구다.

---

## 2. 제품 정의

### 2.1 해결하려는 문제

단타 매매 사용자는 짧은 시간 안에 많은 정보를 동시에 처리해야 한다. 종목 가격이 움직이는 것만 확인해서는 충분하지 않고, 실제 자금이 유입되는지와 매수 체결이 우세한지를 함께 판단해야 한다. 진입 후에는 주문이 정상적으로 접수됐는지, 일부만 체결된 것은 아닌지, 목표 가격과 손절 가격에 도달했는지를 계속 확인해야 한다.

구체적으로 사용자가 장중에 반복해서 확인하는 정보는 다음과 같다.

- 짧은 시간에 가격이 급변한 종목
- 실제로 자금이 유입되는지 여부
- 매수 체결 우위와 체결강도
- 현재 주문, 부분 체결, 미체결 상태
- 보유 종목별 익절·손절·고점 추적 조건
- 주문 이후 가격 변화에 따른 정정·취소 판단

일반 HTS는 주문과 차트 기능이 강력하지만, 사용자가 원하는 감지 조건과 자동 대응 절차가 여러 화면에 분산되는 경우가 많다. 여러 종목을 동시에 감시하면서 동일한 규칙을 적용하려면 사용자가 계속 화면을 전환하고 주문 상태를 직접 대조해야 한다. 나비트레이딩은 이 반복 작업을 전략이라는 단위로 묶고, 감지부터 청산까지 같은 기준이 이어지게 하는 것을 목표로 한다.

나비트레이딩은 이 과정을 다음 흐름으로 단순화한다.

`실시간 체결 수신 → 조건 평가 → 급등 신호 생성 → 사용자 확인 또는 자동 주문 → 체결 추적 → 포지션 위험 관리 → 청산 → 결과 분석`

### 2.2 목표 사용자

첫 번째 목표 사용자는 장 시작 직후 1~5분 동안 발생하는 급격한 수급 변화를 빠르게 확인하려는 단타 사용자다. 이 사용자는 단순 상승률보다 거래대금과 체결강도 같은 실제 수급 지표를 중요하게 본다.

두 번째 목표 사용자는 여러 종목을 동시에 감시하면서 같은 매매 원칙을 일관되게 적용하려는 사용자다. 사람이 직접 감시하면 종목마다 대응 시점이 달라질 수 있지만, 전략을 사용하면 동일한 기준으로 신호와 주문을 처리할 수 있다.

세 번째 목표 사용자는 감지 구간, 거래대금, 체결강도, 익절·손절과 같은 파라미터를 직접 조정하고 결과를 비교하려는 숙련 사용자다. 이 사용자에게는 단순한 자동 주문보다 조건별 통과 여부와 사후 분석 기능이 중요하다.

마지막으로 증권사 API를 연동하고 검증해야 하는 내부 개발자와 QA 담당자도 주요 사용자다. 이들을 위해 별도의 API 급등 감지 검증 앱을 제공한다.

### 2.3 비목표

나비트레이딩은 수익이나 종목 성과를 보장하는 서비스가 아니다. 시스템은 설정된 조건을 일관되게 실행하고 사용자의 대응 시간을 줄이는 도구이며, 시장 위험 자체를 제거할 수는 없다.

또한 거래소에 직접 연결하는 초고빈도매매 시스템을 목표로 하지 않는다. 증권사 OpenAPI를 사용하기 때문에 해당 API의 속도와 호출·구독 한도를 따라야 한다. 이 한도를 우회하는 기능도 제공하지 않는다. 향후 AI 기능을 추가하더라도 사용자의 투자 판단을 완전히 대신하는 범용 투자자문 시스템으로 정의하지 않는다.

---

## 3. 현재 제품 기능

현재 제품은 시장을 감시하는 화면과 실제 주문을 관리하는 화면이 분리되어 있으면서도, 하나의 전략 ID를 통해 신호와 주문, 체결, 포지션을 연결하는 구조다. 사용자는 홈 화면에서 시장 흐름을 확인하고, 급등 감지 화면에서 발생한 신호를 검토하며, 거래 관리 화면에서 실제 주문과 잔고를 확인할 수 있다.

### 3.1 시장 감시

시장 감시 화면은 사용자가 지금 시장에서 주목해야 할 종목을 빠르게 비교하는 영역이다. 시장 중심 종목, 관심종목, 최근 조회 종목을 그룹 단위로 표시하고, 각 종목의 현재가와 등락률, 거래대금 정보를 실시간으로 갱신한다. 같은 종목이 여러 그룹에 포함되더라도 시세 구독과 조회는 한 번만 수행하고 화면에서 결과를 재사용하는 구조가 바람직하다.

- 시장 중심 종목 그룹 조회
- 한국투자증권 급등주 범위 감시
- 개별 종목, 관심 그룹, 두 범위의 결합 감시
- KRX 정규장, NXT 프리·애프터마켓 범위 설정
- 종목 검색, 관심종목 추가·제거, 최근 본 종목
- 실시간 현재가, 등락률, 틱 거래대금, 누적 거래대금
- 일·주·월·분·틱 차트
- 뉴스와 관련 종목, 관심종목 추천

### 3.2 급등 감지

급등 감지는 이 제품의 핵심 기능이다. 단순히 전일 종가보다 많이 오른 종목을 찾는 것이 아니라, 사용자가 지정한 짧은 구간 안에서 가격과 수급이 동시에 변했는지를 판단한다. 현재 전략은 아래 조건을 조합할 수 있으며, 선택된 필터의 조건은 기본적으로 동시에 충족해야 신호가 생성된다.

예를 들어 “0.5초 동안 가격이 0.7% 이상 상승하고, 해당 구간 거래대금이 5천만 원 이상이며, 체결강도가 120 이상”이라는 전략을 만들 수 있다. 이때 세 조건 중 하나라도 부족하면 신호를 만들지 않는다. 따라서 조건을 지나치게 엄격하게 설정하면 시장에서 눈에 띄는 움직임이 있어도 신호가 0건이 될 수 있다. 반대로 조건을 너무 느슨하게 설정하면 의미 없는 작은 움직임까지 대량으로 감지될 수 있다.

| 구분 | 파라미터 | 의미 |
|---|---|---|
| 기본 | 감지 구간 | 상승률과 구간 수급을 계산할 시간 범위. 0은 최근 체결 기준 즉시 평가 |
| 기본 | 구간 상승률 | 기준 가격 대비 최소 상승률 |
| 기본 | 최소 체결 수 | 계산 구간 안에 필요한 최소 체결 건수 |
| 수급 | 거래대금 | 최근 개별 체결 또는 선택 필터가 요구하는 최소 거래대금 |
| 수급 | 거래량 | 최소 체결 수량 |
| 수급 | 총 거래대금 | 당일 누적 거래대금 하한 |
| 수급 | 체결강도 | 매수 체결량 대비 매도 체결량의 비율. 100은 양쪽이 동일한 수준 |
| 수급 | 매수 체결 비율 | 구간 내 전체 체결 중 매수 주도 체결 비율 |
| 수급 | 순매수 거래량 | 매수 체결량에서 매도 체결량을 뺀 값 |
| 수급 | 구간 거래대금 | 설정 구간에 유입된 거래대금 합계 |
| 수급 | 구간 거래량 | 설정 구간에 체결된 수량 합계 |
| 수급 | 거래대금 가속도 | 직전 구간 대비 현재 구간 거래대금 배수 |
| 범위 | 감시 범위 | 시장 중심, KIS 급등주, 개별 종목, 관심 그룹 등 |
| 시간 | 거래 세션 | KRX, NXT, 전체 |
| 시간 | 자동 실행 시간 | 신규 감지·매수를 허용할 시작·종료 시각 |

### 3.3 주문

신호가 생성된 다음에는 전략의 실행 방식에 따라 주문 단계로 이동한다. 사용자 확인 방식에서는 신호 팝업에 감지가, 현재가, 감지 체결대금, 매수 수량과 예상 주문금액을 표시하고 사용자가 승인한 경우에만 주문한다. 완전 자동 방식에서는 일일 손실, 최대 보유 종목, 재진입 대기와 같은 위험 제한을 확인한 뒤 주문을 전송한다.

지정가 주문은 사용자가 원하는 가격을 통제할 수 있지만 급변하는 시장에서 미체결될 가능성이 높다. 시장가는 체결 가능성이 높지만 슬리피지가 커질 수 있다. 지정가 후 시장가는 먼저 지정가로 체결을 시도하고 정해진 조건에서 시장가로 전환하는 절충 방식이다.

- 사용자 확인 방식
- 완전 자동 방식
- 금액, 수량, 계좌 비율 기준 주문
- 매수 오프셋 적용
- 시장가
- 지정가
- 지정가 후 시장가
- 주문 전 매수 가능 금액·수량 조회
- 미체결 주문 정정·취소
- 부분 체결 진행 상태 표시
- 동일 종목 보유·주문 여부 확인

### 3.4 포지션 관리

매수 체결이 완료되면 해당 포지션에는 신호를 발생시킨 전략이 기본으로 연결된다. 이후 사용자는 필요에 따라 다른 전략으로 변경할 수 있다. 시스템은 현재가와 평균단가를 비교하여 고정 익절, 손절, 시간 청산, 고점 추적 조건을 평가한다.

고점 추적은 단순 익절과 다르게 작동한다. 먼저 설정한 수익률에 도달해야 추적이 시작되고, 그 이후 기록된 최고 가격에서 일정 비율 이상 하락했을 때 매도를 시도한다. 추적 시작 전에 하락한 경우에는 고점 추적이 아니라 고정 손절이나 다른 청산 규칙이 담당해야 한다.

- 고정 익절
- 고정 손절
- 시간 청산
- 고점 추적 시작 수익률
- 고점 대비 하락률
- 하락 확인 체결 수
- 최소 유지 시간
- 매도 접수 후 추가 하락 시 가격 정정
- 종목별 전략 연결·변경
- 손절 일시 중지
- 자동 매도 일시 중지
- 수동 추적 시작·중지
- 바로매도 및 일괄매도

### 3.5 거래 관리

거래 관리 화면은 증권사 계좌의 현재 상태를 확인하는 기준 화면이다. 예수금과 보유 종목뿐 아니라 주문 접수, 부분 체결, 미체결, 정정, 취소, 최종 체결까지 하나의 흐름으로 보여줘야 한다. 화면에 표시되는 내부 상태가 증권사 원장과 다르면 사용자가 잘못된 결정을 내릴 수 있으므로, 이 영역은 다른 부가 기능보다 우선해서 안정화해야 한다.

- 계좌 요약
- 예수금, 매입금액, 평가금액, 평가손익
- 실현손익과 추정 비용
- 보유 종목
- 전체 주문
- 미체결
- 체결
- 실시간 체결 알림
- 전략별 매수금액, 매도금액, 실현손익, 수익률
- 기간별 분석

### 3.6 보안 및 기록

계좌 연결에 필요한 인증정보는 서비스 운영자에게도 민감한 데이터다. 현재 코드는 API Key, App Secret, 계좌번호, HTS ID를 저장할 때 무작위 IV를 사용하는 AES-256-GCM 방식으로 암호화한다. 데이터를 읽을 때만 서버 내부에서 복호화하며 화면에는 전체 값을 반환하지 않는다.

주문과 전략 변경은 감사 테이블에 기록하고, 사용자의 주요 행동은 날짜별 JSONL 파일에 비동기로 저장한다. 이 기록은 장애 발생 시 어떤 요청이 먼저 일어났는지 확인하는 데 사용한다. 다만 로그가 실시간 처리에 영향을 주지 않도록 큐 크기와 보관 기간, 압축·삭제 정책을 함께 운영해야 한다.

- API Key, App Secret, 계좌번호, HTS ID를 AES-256-GCM으로 암호화 저장
- 사용자 세션과 계좌별 데이터 분리
- 주문·전략·신호 관련 감사 기록
- 사용자 활동을 날짜별 JSONL 파일로 비동기 기록
- 화면에는 인증정보 전체 값을 노출하지 않고 마스킹

---

## 4. 현재 시스템 구조

현재 시스템은 브라우저 화면, Fastify API, 자동매매 엔진, MySQL, Redis, 증권사 API로 구성된다. 브라우저는 사용자 입력과 상태 표시를 담당하고, 실제 감지와 주문은 서버에서 수행한다. 따라서 사용자가 다른 화면으로 이동하더라도 자동매매 엔진은 계속 동작한다.

### 4.1 구성

| 계층 | 현재 기술 |
|---|---|
| 사용자 화면 | React 19, TypeScript, Vite, MUI |
| 차트 | Lightweight Charts |
| API | Fastify, Node.js |
| 영속 데이터 | MySQL |
| 실시간 캐시·이벤트 | Redis |
| 실시간 화면 | WebSocket |
| 증권사 | 한국투자증권 OpenAPI, 향후 추가 공급자 구조 |
| 운영 프로세스 | PM2 |

### 4.2 주요 데이터 흐름

일반적인 거래 한 건은 다음 순서로 처리된다. 이 흐름 중 어느 한 단계라도 누락되면 신호는 보이지만 주문이 나가지 않거나, 주문은 체결됐지만 잔고에 반영되지 않는 문제가 발생할 수 있다.

1. 서버가 증권사 WebSocket에서 실시간 체결을 수신한다.
2. 종목별 최신 틱과 구간 통계를 갱신한다.
3. 활성 전략의 범위와 수급 조건을 평가한다.
4. 조건이 충족되면 신호를 DB에 저장하고 Redis/WebSocket으로 전달한다.
5. 사용자 확인 전략은 승인 대기 상태가 된다.
6. 완전 자동 전략은 위험 한도와 예외 조건을 검사한 뒤 주문한다.
7. 주문 접수 후 증권사 체결 통지와 조회 API를 함께 사용해 상태를 추적한다.
8. 체결된 매수는 포지션과 전략에 연결된다.
9. 실시간 가격에 따라 익절·손절·고점 추적·시간 청산을 평가한다.
10. 주문·체결·잔고 결과를 화면과 분석 지표에 반영한다.

### 4.3 현재 구조의 중요한 제약

현재 `server/index.ts`는 API 서버와 자동매매 엔진을 같은 프로세스에서 시작한다. 따라서 PM2의 단순 클러스터 모드로 인스턴스를 늘리면 각 인스턴스가 자동매매 엔진과 증권사 WebSocket을 중복 실행할 수 있다. 이는 중복 주문과 구독 초과를 발생시킬 수 있어 허용하면 안 된다.

단순히 PM2 인스턴스 수를 늘리는 방식으로는 이 문제를 해결할 수 없다. API 처리량은 늘어날 수 있지만 자동매매 엔진도 여러 개가 되기 때문이다. 멀티코어 사용은 다음과 같이 역할을 분리한 후 적용해야 한다.

- API Worker: 여러 프로세스로 확장 가능
- Trading Leader: 계좌별 하나만 실행
- 실시간 시세 수집기: 공급자·계좌 정책에 맞춰 단일 또는 제한된 수로 실행
- Redis: Worker와 Trading Leader 사이의 명령·이벤트 전달
- 분산 잠금과 fencing token: 중복 주문 방지

---

## 5. 장점

나비트레이딩의 가장 큰 장점은 감지 화면, 주문 화면, 잔고 화면을 각각 따로 만든 것이 아니라 하나의 전략을 중심으로 전체 거래 흐름을 연결했다는 점이다. 사용자는 단순히 “급등 종목이 나타났다”는 알림만 받는 것이 아니라, 어떤 조건으로 감지됐고 어떤 주문으로 이어졌으며 이후 어떤 기준으로 청산됐는지를 같은 흐름 안에서 확인할 수 있다.

### 5.1 기능적 장점

첫째, 단순 등락률만 보는 일반적인 급등 목록과 달리 실제 체결 수급을 함께 평가할 수 있다. 가격은 올랐지만 거래가 거의 없는 종목과, 짧은 시간에 실제 매수 자금이 집중된 종목을 구분할 수 있다는 의미다. 거래대금, 거래량, 체결강도, 매수 체결 비율 등을 함께 사용하면 감지 기준을 사용자의 매매 성향에 맞게 구체화할 수 있다.

둘째, 같은 시스템 안에서 사용자 확인과 완전 자동 방식을 전략별로 나눌 수 있다. 확신이 낮은 전략은 팝업을 보고 직접 승인하고, 충분히 검증된 전략만 자동 실행하도록 단계적으로 운용할 수 있다. 모의투자와 실전투자도 같은 인터페이스에서 명확하게 구분할 수 있어 전략 검증에서 실제 운용으로 넘어가는 과정이 비교적 자연스럽다.

셋째, 신호·주문·체결·포지션·청산을 하나의 전략 ID로 연결할 수 있다. 전략 이름과 색상을 신호 팝업, 보유 종목, 체결 이력과 분석 화면에 일관되게 표시하면 사용자는 어떤 전략이 실제 손익에 기여했는지 추적할 수 있다.

### 5.2 기술적 장점

기술적으로는 React, Fastify, MySQL, Redis가 각각 화면, API, 영속 데이터, 실시간 캐시와 이벤트 역할을 담당한다. 전략과 주문 데이터가 DB에 남기 때문에 장애 후 복구와 사후 분석이 가능하고, Redis와 WebSocket을 사용해 브라우저가 모든 데이터를 반복 조회하는 부담도 줄일 수 있다.

계좌 인증정보를 평문으로 저장하지 않고 인증 암호화 방식으로 보관하는 점도 중요한 기반이다. 또한 계좌와 공급자 구조가 분리되어 있어 향후 다른 증권사 API를 추가할 수 있는 여지가 있다. 활동 로그와 감사 테이블 역시 단순 운영 기록을 넘어 주문 사고의 원인을 시간 순서대로 재구성하는 근거가 된다.

### 5.3 사업적 장점

사업적으로는 현재 웹 기반 구조를 SaaS나 사내 전용 서비스로 제공할 수 있고, 핵심 거래 엔진을 분리하면 사용자 PC 설치형 제품으로도 확장할 수 있다. 기본 감지 기능, 자동 주문, 다중 계좌, 리플레이와 분석 기능을 라이선스 등급별로 구성할 수 있으며, 검증된 전략 템플릿도 별도 상품 요소가 될 수 있다.

별도로 제안하는 API 검증 앱은 개발 단계에서만 사용하는 일회성 도구가 아니다. 내부 QA가 신규 API와 배포 버전을 검증하는 데 사용할 수 있고, 고객 지원 과정에서는 특정 사용자의 연결·구독·주문 문제를 재현하는 진단 도구로 활용할 수 있다.

---

## 6. 단점과 구조적 한계

이 제품의 단점은 화면 기능이 부족해서 발생하는 문제가 아니라, 외부 증권사 API를 이용하는 실시간 거래 시스템이라는 구조 자체에서 나온다. 특히 “빠르게 보이는 화면”과 “정확하게 체결되는 주문”은 서로 다른 문제이므로, 응답 속도를 높이는 과정에서도 원장 정합성과 중복 주문 방지를 우선해야 한다.

### 6.1 외부 API 의존

증권사 API에는 REST 호출 횟수와 WebSocket 구독 종목 수에 제한이 있다. 시스템 내부 처리 속도가 아무리 빨라도 이 한도를 넘으면 구독이나 주문 조회가 거부된다. 체결 통지와 조회 API의 반영 시점이 서로 다를 수도 있어, 한쪽 데이터만 믿으면 화면 상태가 잠시 틀어질 수 있다.

또한 KRX 정규장과 NXT 프리·애프터마켓은 거래소 코드와 주문 가능 시간, 지원 주문 방식이 다르다. 증권사 장애나 사용자 네트워크 단절도 시스템이 직접 통제할 수 없다. 따라서 외부 연결이 불안정할 때 신규 매수를 중단하고, 이미 보유한 포지션은 별도 복구 경로로 관리하는 설계가 필요하다.

### 6.2 전략 검증 부족

현재까지 축적된 기록은 기능이 동작하는지 확인하는 데는 도움이 되지만 장기 수익성을 증명하기에는 부족하다. 급등을 정확히 감지하는 것과 감지 후 수익을 내는 것은 별개의 문제다. 급등 직후에는 주문 가격이 빠르게 변해 예상보다 높은 가격에 체결될 수 있고, 이미 상승이 끝난 시점에 진입할 위험도 있다.

파라미터가 많다는 점은 세밀한 전략을 만들 수 있다는 장점인 동시에 과최적화 위험이기도 하다. 과거 몇 건에 잘 맞는 값이 다음 거래일에는 작동하지 않을 수 있으며, 사용자가 단위나 조건 조합을 잘못 이해하면 신호가 지나치게 많거나 아예 발생하지 않을 수 있다.

### 6.3 실시간 상태 복잡성

한 주문은 접수 직후 전량 체결될 수도 있지만 일부만 체결된 채 정정이나 취소 요청과 겹칠 수도 있다. 이 과정에서 브라우저, MySQL, Redis, 증권사 원장의 상태가 잠시 서로 달라지는 것은 피하기 어렵다. 문제는 이 차이를 정상적인 지연으로 판단할지 실제 장애로 판단할지 구분하는 것이다.

응답이 늦다고 같은 주문을 단순히 다시 보내면 중복 주문이 만들어질 수 있다. 따라서 모든 주문에 고유한 의도 ID를 부여하고, 재시도하더라도 같은 의도는 한 번만 실행되도록 만드는 멱등성 보장이 필수다.

### 6.4 운영 복잡성

현재처럼 API 서버, 자동매매 엔진, 실시간 수집기가 한 프로세스에 결합되어 있으면 한 기능의 과부하가 전체 서비스에 영향을 줄 수 있다. 반대로 단순히 프로세스 수를 늘리면 자동매매 엔진이 중복 실행될 위험이 있다. 멀티코어 확장은 역할 분리와 계좌별 리더 선출을 먼저 구현한 후 진행해야 한다.

상세 활동 로그와 원본 틱은 장애 분석에 필요하지만 제한 없이 쌓으면 디스크와 I/O를 소모한다. 감시 종목 수가 늘수록 구독 관리, 메모리 사용량, 브라우저 이벤트 수도 함께 증가하므로 보관 기간과 이벤트 상한을 제품 정책으로 정해야 한다.

---

## 7. 지금까지의 운영 결과

현재까지의 운영은 상용 성과를 증명하기 위한 단계라기보다 실제 증권사 환경에서 시스템의 약점을 찾는 검증 과정이었다. 이 과정에서 계좌 연결부터 자동 주문과 청산까지 기본 흐름이 실제로 이어진다는 점은 확인했지만, 동시에 무인 운용 전에 해결해야 할 병목과 상태 불일치도 명확하게 확인했다.

### 7.1 확인된 성과

실제 한국투자증권 계좌와 모의투자 계좌에서 잔고를 조회했고, 실시간 체결 데이터를 이용해 급등 신호가 생성되는 사례를 확인했다. 사용자 승인 주문과 완전 자동 주문 모두 접수와 체결까지 이어진 사례가 있으며, 부분 체결과 미체결 주문을 화면에 표시하고 정정·취소를 요청하는 기본 흐름도 구현됐다.

매수 체결 후 해당 종목에 전략을 연결하고 익절·손절·고점 추적을 평가하는 구조도 실제 데이터와 연결됐다. 체결 내역에는 전략 정보가 함께 저장되므로 향후 전략별 매수·매도 금액과 손익을 비교할 수 있는 기반도 마련됐다.

### 7.2 운영 중 확인된 수치와 현상

운영 서버 점검 표본에서 다음 현상이 확인됐다.

- 단일 Node 프로세스가 한 CPU 코어 기준 약 70% 수준까지 사용한 사례
- 프로세스 RSS 메모리가 약 700MB 이상으로 증가한 사례
- PM2 로그 파일이 100MB 이상 누적된 사례
- 하루 약 4만 건 이상의 HTTP 활동 로그가 기록된 사례
- 종목 행 조회 요청 하나가 약 95초까지 지연된 사례
- KIS WebSocket에서 `MAX SUBSCRIBE OVER`, `ALREADY IN SUBSCRIBE`, `UNSUBSCRIBE ERROR` 발생
- 약 40개 안팎의 종목 요청 중 일부가 실제 구독되지 않는 사례
- 엄격한 수급 전략이 활성 상태였지만 신호가 0건인 사례

이 수치는 특정 시점의 운영 표본이며 전체 기간 평균은 아니다. 정식 KPI로 사용하려면 모니터링 시스템에서 기간별로 다시 집계해야 한다.

이 표본이 보여주는 핵심은 서버 자원이 단순히 부족하다는 것이 아니다. 종목 조회가 오래 걸리는 동안 WebSocket 재연결과 구독 요청이 겹치고, 활동 로그와 DB 조회가 한 프로세스에서 함께 처리되면서 전체 응답성이 낮아지는 복합 병목이 존재한다. 따라서 서버 사양을 올리는 것만으로는 근본적으로 해결되지 않으며, 실시간 수집·자동매매·일반 API의 역할을 분리해야 한다.

### 7.3 운영 결론

운영 결과를 종합하면 현재 시스템은 “기능 시연이 가능한 프로토타입”에서는 상당히 진전됐지만 “장시간 방치해도 안전한 자동매매 제품”에는 아직 도달하지 않았다. 현재까지 확인된 핵심 결론은 다음과 같다.

1. 기능은 연결됐지만 무인 실전 운용 안정성은 아직 충분하지 않다.
2. 병목은 CPU 하나만의 문제가 아니라 증권사 구독 한도, API 지연, DB 요청, 로그, 재연결이 결합된 문제다.
3. 실시간 차트가 보인다고 자동 감지에 필요한 WebSocket 틱이 반드시 수신된 것은 아니다.
4. 주문·체결 상태는 증권사 원장을 최종 기준으로 재조정해야 한다.
5. 현재 손익 데이터만으로 전략 수익성을 주장하면 안 된다.
6. 자동매매 확대 전 리플레이·Shadow Mode·회귀 테스트가 필요하다.

### 7.4 수익성 평가

현재 데이터는 실현손익, 평가손익, 수수료·세금 추정 기준이 여러 차례 조정됐고 주문 이력이 계좌 원장과 불일치한 사례가 있었다. 따라서 현재 화면의 누적 손익만으로 승률이나 기대수익을 확정할 수 없다.

이는 현재 전략이 반드시 손실 전략이라는 뜻도, 반대로 수익 전략이라는 뜻도 아니다. 계산 기준과 원본 거래 이력이 안정되지 않은 상태에서는 어느 쪽도 객관적으로 증명할 수 없다는 의미다. 향후에는 증권사 체결 원장을 기준 데이터로 고정하고, 전략 버전과 신호 시점의 틱을 함께 보관한 뒤 같은 기준으로 다시 산출해야 한다.

정식 운영 성과는 아래 기준을 충족한 이후 산출한다.

- 증권사 체결 원장과 내부 체결 내역 100% 대조
- 계좌별 주문·체결·손익 완전 분리
- 수수료와 세금 기준 고정
- 신호 발생 시점의 원본 틱 보관
- 전략 버전별 파라미터 스냅샷 저장
- 최소 20거래일 이상의 모의·Shadow Mode 결과

---

## 8. 주요 버그 및 리스크 목록

버그의 우선순위는 화면에서 얼마나 자주 보이는지가 아니라 실제 주문과 자금에 미치는 영향으로 나눈다. P0는 실거래 손실이나 중복 주문으로 이어질 수 있어 실전 자동매매 전에 반드시 해결해야 하는 항목이다. P1은 상용 베타의 안정성과 운영 비용에 직접 영향을 주며, P2는 기능 자체보다 사용자의 판단과 조작 편의성을 개선하는 항목이다.

### 8.1 P0: 실거래 전 반드시 해결

| 문제 | 영향 | 필요한 조치 |
|---|---|---|
| 주문 상태와 증권사 원장 불일치 | 이미 체결·취소된 주문을 미체결로 표시하거나 반대 상황 발생 | 체결 통지와 조회 API를 조합한 계좌별 Reconciliation Worker 구축 |
| 부분 체결 정정·취소 오류 | 잔여 수량 방치 또는 중복 처리 | 원주문·정정주문·취소주문 연결 모델과 잔여 수량 검증 |
| 익절·손절·고점 추적 미작동 사례 | 계획보다 큰 손실 발생 가능 | 조건 평가 로그, 고점 영속화, 미체결 매도 추적, 강제 시장가 전환 |
| 실시간 종목 구독 누락 | 급등 계산 자체가 실행되지 않음 | 구독 관리자, 확인 응답, 자동 재배치, 구독 누락 경보 |
| 중복 자동매매 엔진 가능성 | 동일 신호에 중복 주문 가능 | Trading Leader 분리, 분산 잠금, 주문 멱등키 |
| 계좌 간 이력 혼합 위험 | 다른 계좌의 주문·손익이 표시될 수 있음 | 모든 조회·Redis Key·WebSocket 이벤트에 account_id 강제 |
| 손익 기준 불일치 | HTS와 다른 실현손익 표시 | 증권사 원장값 우선, 내부 계산값은 “추정”으로 분리 |

### 8.2 P1: 상용 베타 전 해결

| 문제 | 영향 | 필요한 조치 |
|---|---|---|
| WebSocket 재연결 반복 | 화면 갱신 중단, 서버 부하 | 지수 백오프, 단일 연결 재사용, heartbeat, 연결 상태 표시 |
| API 호출 제한 초과 | 주문·조회 거부 | 계좌·TR ID별 rate limiter와 우선순위 큐 |
| 화면 메모리 증가 | 브라우저 OOM | 이벤트 상한, 가상 스크롤, 오래된 틱 제거, 중복 구독 제거 |
| 시세·잔고 갱신 지연 | 잘못된 사용자 판단 | 서버 push 우선, HTTP fallback, 마지막 수신 시각 표시 |
| 차트 틱·OHLC·거래대금 불일치 | 분석 신뢰 저하 | 동일 원본 틱 사용, span별 집계 테스트 |
| 신호 팝업 중복·겹침 | 주문 실수 가능 | 종목+전략 기준 병합, 최대 표시 수, 대기 큐 |
| OAuth·CORS·리다이렉트 설정 오류 | 로그인 실패 | 운영 도메인별 구성 검증과 배포 전 smoke test |
| DB 마이그레이션 누락 | 서버 시작·주문 실패 | 버전 관리형 migration과 시작 전 schema check |

### 8.3 P2: 사용성 개선

사용성 문제는 P0보다 위험도가 낮지만 주문 화면에서는 단순 미관 문제가 아니다. 버튼 크기, 열 너비, 팝업 위치가 일관되지 않으면 사용자가 급한 상황에서 잘못된 가격이나 수량을 선택할 수 있다. 따라서 아래 항목도 거래 안전성 검증과 함께 개선한다.

- 버튼·테이블 높이와 열 너비 통일
- 긴 상태 라벨 줄바꿈 방지
- 모바일·작은 화면 오버플로 개선
- 수동 매매 가격·수량 프리셋 정리
- 모든 팝업의 키보드 단축키 일관화
- 전략 파라미터 설명과 단위 표시
- 오류 코드 대신 사용자 행동이 포함된 한국어 메시지 제공

---

## 9. 개선 원칙

앞에서 확인한 문제를 개별 오류 수정으로만 대응하면 비슷한 장애가 다른 경로에서 반복될 가능성이 높다. 이후 개발은 아래 네 가지 원칙을 공통 기준으로 삼아야 한다. 새로운 주문 기능이나 전략을 추가할 때도 이 원칙을 통과하지 못하면 실전 기능으로 활성화하지 않는다.

### 9.1 증권사 원장이 최종 기준

내부 DB는 빠른 화면 표시와 전략 상태 복구를 위한 저장소이지 실제 계좌 원장을 대체하지 않는다. 체결 수량, 잔고, 주문 가능 수량이 다를 경우 증권사 조회 결과를 최종 기준으로 사용하고 내부 상태를 교정해야 한다. 화면에는 마지막 원장 확인 시각을 함께 표시해 데이터의 신선도를 사용자가 판단할 수 있게 한다.

### 9.2 주문은 멱등적으로 처리

하나의 신호에는 하나의 주문 의도 ID를 발급한다. 사용자가 버튼을 여러 번 누르거나 네트워크 재시도, 프로세스 재시작이 발생해도 같은 의도 ID로는 두 번 주문하지 않는다. 주문 결과를 받지 못한 경우에도 즉시 새 주문을 만드는 대신 증권사 원장을 조회해 기존 주문 존재 여부부터 확인한다.

### 9.3 실시간과 복구 경로를 분리

정상 상황에서는 WebSocket 이벤트를 사용해 체결과 잔고를 즉시 갱신한다. 그러나 실시간 이벤트는 네트워크 문제로 누락될 수 있으므로 조회 API를 이용한 복구 경로가 항상 함께 있어야 한다. 실시간 연결이 일정 시간 이상 끊기면 시스템은 이를 단순 화면 오류로 취급하지 않고 신규 매수를 차단해야 한다.

- 정상 경로: WebSocket 체결 통지로 즉시 갱신
- 복구 경로: 조회 API로 누락·불일치 교정
- 장애 경로: 신규 매수 중지, 보유 포지션 위험 관리 유지

### 9.4 모든 판단은 재현 가능해야 함

급등 신호나 자동 청산이 발생했을 때 “그때 가격이 그랬다”는 설명만으로는 문제를 검증할 수 없다. 전략이 사용한 원본 틱과 기준값, 실제값, 통과 여부를 함께 저장해야 같은 입력으로 판단을 다시 실행할 수 있다.

신호마다 다음 데이터를 함께 저장한다.

- 전략 ID와 전략 버전
- 평가 시각
- 기준 가격과 현재 가격
- 사용한 틱 범위
- 거래대금, 거래량, 총 거래대금
- 체결강도, 매수 비율, 순매수량
- 각 조건의 기준값·실제값·통과 여부
- 주문 가격과 오프셋

---

## 10. 향후 기능

향후 기능은 현재 거래 정합성이 안정된 이후 단계적으로 추가한다. 특히 포지션 노출을 늘리는 물타기와 불타기는 편의 기능이 아니라 위험을 확대할 수 있는 주문 기능이므로, 일반 화면 기능과 같은 기준으로 출시하면 안 된다. 먼저 Shadow Mode와 리플레이를 통해 전략 결과를 검증한 뒤 제한적으로 제공하는 것이 적절하다.

### 10.1 물타기

물타기는 보유 종목의 가격이 내려갔을 때 추가 매수하여 평균단가를 낮추는 기능이다. 평균단가가 낮아지면 작은 반등에도 손익분기점에 도달할 수 있지만, 예상과 달리 하락이 계속되면 손실 금액과 계좌 노출이 함께 커진다. 따라서 물타기를 “손실을 줄이는 기능”으로 설명해서는 안 되며, 기본값은 반드시 비활성화한다.

예를 들어 최초 50만 원을 매수한 종목이 2% 하락했을 때 다시 50만 원을 매수하면 평균단가는 낮아지지만 투자금은 두 배가 된다. 이후 다시 2% 하락하면 평균단가 기준 손실률은 완화될 수 있어도 실제 손실 금액은 더 커질 수 있다. 이 때문에 단계별 추가 매수 조건뿐 아니라 종목당 최대 투자금과 계좌 전체 노출 한도가 반드시 필요하다.

#### 제안 설정

- 사용 여부
- 최대 추가 매수 횟수
- 단계별 손실률
- 단계별 매수 금액 또는 최초 주문 대비 비율
- 추가 매수 간 최소 대기 시간
- 최소 체결강도
- 최소 매수 체결 비율
- 추가 매수 후 통합 평균단가
- 종목당 최대 투자금
- 계좌 전체 최대 노출액
- 일일 최대 손실
- 추가 매수 금지 시간
- 거래정지·VI·상하한가·시세 단절 시 금지

#### 필수 안전 규칙

- 이미 미체결 매수 주문이 있으면 추가 주문 금지
- 손절 주문 진행 중이면 추가 주문 금지
- 일일 손실 한도 도달 시 금지
- 증권사 잔고 동기화 완료 전 금지
- 최대 단계 도달 시 더 이상 추가하지 않음
- 물타기 전용 손절 기준을 별도로 설정
- 실전 활성화 전 모의·리플레이 검증 필수

### 10.2 불타기

불타기는 수익 중인 종목의 상승 추세가 이어질 때 추가 진입하는 기능이다. 손실 포지션을 늘리는 물타기와 달리 확인된 추세에 자금을 더 배분한다는 장점이 있지만, 고점 부근에서 추가 매수하면 기존 수익을 빠르게 반납할 수 있다. 고점 추적과 함께 사용할 경우 추가 체결마다 총 노출액과 평균단가, 추적 기준 고점을 다시 계산해야 한다.

### 10.3 Shadow Mode

Shadow Mode는 실제 주문을 내지 않고 “이 시점에 어떤 가격과 수량으로 주문했을지”만 기록하는 운용 방식이다. 실시간 시장 데이터와 동일한 조건을 사용하되 자금 위험 없이 감지 누락, 주문 지연, 전략 성능을 확인할 수 있다. 신규 전략은 모의투자보다 먼저 Shadow Mode로 검증하고, 결과가 일정 기준을 충족한 경우에만 모의·실전 주문 단계로 승격한다.

### 10.4 틱 리플레이·백테스트

틱 리플레이는 과거 데이터를 단순 차트로 보는 기능과 다르다. 저장된 체결을 실제 발생 순서대로 다시 흘려보내고, 당시 전략 코드가 어떤 판단을 했는지 재실행하는 기능이다. 운영 중 신호가 누락됐거나 예상보다 늦게 청산된 경우에도 같은 데이터를 재생해 원인을 확인할 수 있다.

- 저장된 틱을 원래 시간 순서대로 재생
- 전략 조건별 통과·탈락 표시
- 증권사 호가 단위를 반영한 가상 체결
- 슬리피지, 수수료, 세금 적용
- 전략 버전 비교
- 감지 후 10초·30초·1분·5분 최대 상승과 최대 하락 분석

### 10.5 전략 템플릿

파라미터가 많은 제품에서는 사용자가 모든 값을 처음부터 입력하도록 하면 설정 오류가 늘어난다. 목적별 기본 템플릿을 제공하고, 각 템플릿이 어떤 시장 상황을 가정하는지 설명한 뒤 사용자가 필요한 값만 조정하도록 구성한다.

- 장 시작 5분 수급형
- 단일 대량 체결형
- 거래대금 가속형
- 체결강도 돌파형
- 보수형 사용자 확인
- NXT 전용

템플릿은 수익을 보장하지 않으며, 계좌와 시장 상태에 맞게 사용자가 최종 승인해야 한다.

### 10.6 위험 관리

위험 관리는 개별 전략보다 상위에 있어야 한다. 특정 전략이 자동 실행 중이더라도 계좌 전체 손실, 미체결 수, API 지연이 한도를 넘으면 신규 매수는 공통으로 차단한다. 반면 이미 보유한 종목의 조회와 청산 시도는 가능한 범위에서 계속 유지해야 한다.

- 종목별 최대 손실
- 계좌 전체 최대 손실
- 연속 손실 횟수 제한
- 최대 동시 주문 수
- 최대 미체결 주문 수
- API 지연이 기준을 넘으면 신규 매수 차단
- 실시간 시세가 일정 시간 끊기면 신규 매수 차단
- 긴급 전체 취소·전체 청산
- Kill Switch의 서버·클라이언트 이중 제공

### 10.7 분석

분석 화면은 단순 누적 손익만 표시하는 수준을 넘어 “어떤 전략이 어떤 시간대와 시장에서 유효했는지”를 설명해야 한다. 감지는 잘했지만 주문 지연으로 손실이 난 경우와 전략 자체의 진입 기준이 잘못된 경우를 구분할 수 있도록 감지 지연, 체결 지연, 슬리피지를 함께 계산한다.

- 전략별 승률, 평균 수익, 평균 손실, Profit Factor
- 최대 낙폭
- 감지부터 주문까지 지연
- 주문부터 체결까지 지연
- 미체결·정정·취소 비율
- 전략별 슬리피지
- 시간대별 성과
- 감지 조건별 기여도

---

## 11. 로컬 PC 라이선스 제품

### 11.1 목표

장기적으로는 중앙 서버에서 모든 사용자의 주문을 처리하는 구조 대신, 각 사용자 PC에서 거래 엔진이 독립적으로 실행되는 설치형 제품을 제공한다. 사용자는 프로그램을 설치하고 라이선스를 등록한 뒤 자신의 증권사 계정을 연결한다. 화면을 닫아도 Windows 백그라운드 서비스가 실행되어 감지, 주문 추적, 보유 포지션 관리를 계속한다.

이 구조의 핵심 목적은 인증정보와 주문 경로를 사용자 PC 안에 두는 것이다. 중앙 서버 장애가 발생해도 로컬 거래 엔진은 허용된 기간 동안 계속 동작할 수 있으며, 중앙 서버에는 증권사 Secret을 저장하지 않아도 된다.

- 사용자 PC에서 자동매매가 백그라운드 실행
- 브라우저나 화면을 닫아도 전략과 포지션 관리 유지
- 증권사 인증정보를 사용자 PC에만 보관
- 중앙 장애가 있어도 허용된 오프라인 기간 동안 작동
- 라이선스 등급에 따라 계좌 수, 전략 수, 기능 제한

### 11.2 권장 구조

로컬 제품은 현재 웹 서버를 그대로 PC에 복사하는 방식으로 만들면 안 된다. 사용자 한 명이 사용하는 환경에 MySQL과 Redis를 모두 설치하게 하면 설치와 장애 대응이 지나치게 복잡해진다. UI, 거래 서비스, 로컬 저장소, 보안 저장소의 역할을 분리하고 Windows 환경에 맞는 경량 구성을 사용한다.

#### 로컬 구성

- Desktop UI: Tauri 기반 React UI 권장
- Trading Service: 서명된 Node.js sidecar 또는 Windows Service
- Local DB: SQLite WAL 모드
- Local Event Bus: 인프로세스 큐, 필요 시 로컬 Redis 선택
- Secret Vault: Windows DPAPI/Credential Manager
- Auto Update: 서명된 패키지만 설치

현재 서버 코드가 MySQL과 Redis에 강하게 연결되어 있어 바로 로컬 설치형으로 전환할 수는 없다. 저장소와 이벤트 인터페이스를 추상화한 후 SQLite·로컬 이벤트 버스를 추가해야 한다.

#### 중앙 구성

- 라이선스 발급·갱신 API
- 기기 등록·해제
- 라이선스 해지·차단
- 서명된 업데이트 배포
- 선택적 오류 리포트
- 제품 버전·라이선스 상태 확인

중앙 서버는 증권사 주문 경로에 필수로 개입하지 않는다.

사용 흐름은 단순해야 한다. 사용자는 설치 후 로그인하고 라이선스를 활성화한 다음, 증권사 API 정보를 Windows 보안 저장소에 등록한다. 이후 로컬 거래 서비스가 시작되며, 웹과 유사한 UI는 이 서비스의 상태를 조회하고 설정만 변경한다. 프로그램 업데이트는 서명된 패키지만 허용하고, 업데이트 실패 시 이전 버전으로 복구할 수 있어야 한다.

### 11.3 라이선스 방식

라이선스는 단순 문자열 비교가 아니라 서버가 전자서명한 토큰으로 제공한다. 로컬 앱은 공개키만 가지고 서명을 검증하므로 라이선스 서버의 비밀키를 배포할 필요가 없다. 짧은 네트워크 장애 때문에 거래 관리가 즉시 중단되지 않도록 제한된 오프라인 유예기간도 제공한다.

1. 구매 후 사용자 계정에 라이선스를 발급한다.
2. 최초 실행 시 PC의 기기 식별값과 공개키를 등록한다.
3. 서버가 기능 등급, 만료일, 기기 ID가 포함된 라이선스 토큰에 전자서명한다.
4. 로컬 앱은 내장된 공개키로 서명을 검증한다.
5. 일정 기간마다 온라인으로 갱신한다.
6. 네트워크 장애 시 7~30일의 오프라인 유예기간을 제공한다.
7. 해지·도난 기기는 다음 온라인 확인 시 차단한다.

### 11.4 라이선스 등급 예시

| 등급 | 기능 |
|---|---|
| Basic | 사용자 확인 신호, 관심종목, 차트, 1계좌 |
| Pro | 자동 주문, 다중 전략, 리플레이, 2계좌 |
| Enterprise | 다중 계좌, 중앙 관리, 감사 로그, 전용 지원 |

### 11.5 로컬 제품 보안

설치형 제품에서는 중앙 서버보다 사용자 PC의 악성코드와 로그 유출이 더 큰 위협이 될 수 있다. 따라서 인증정보는 `.env`나 일반 설정 파일에 저장하지 않고 Windows 사용자 계정에 귀속된 보안 저장소를 사용한다. 라이선스가 만료되거나 검증 서버가 잠시 응답하지 않더라도 이미 보유한 포지션의 안전한 청산을 막아서는 안 된다.

- API Key와 Secret을 평문 파일이나 `.env`에 저장하지 않음
- DPAPI로 현재 Windows 사용자에 귀속해 암호화
- 로그에서 인증정보·계좌번호·세션 제거
- 실행 파일과 업데이트 패키지 코드 서명
- 자동매매 서비스의 로컬 API는 loopback만 허용
- UI와 서비스 사이에 세션 토큰 적용
- 원격 명령은 기본 비활성화
- 라이선스 검증 실패가 보유 포지션 청산을 방해하지 않도록 설계

---

## 12. API 급등 감지 검증 앱

운영 서비스와 분리된 개발·QA용 애플리케이션으로, 증권사 API 연결부터 실시간 틱 수신, 급등 조건 계산, 모의 주문, 부분 체결, 정정·취소, 원장 대조까지 독립적으로 검증한다.

상세한 개발 목적, 화면 구성, API 기능 범위, 테스트 시나리오와 완료 기준은 별도 문서인 [API 급등 감지 검증 앱 기획서](NAVITR_API_TEST_APP_PLAN.md)에서 관리한다.

---

## 13. 현재 API 인터페이스 요약

현재 서버에는 인증부터 시세, 전략, 주문, 잔고, 운영 제어까지 주요 기능을 호출할 수 있는 API가 구현되어 있다. 아래 목록은 단순 엔드포인트 명세가 아니라 API 검증 앱과 회귀 테스트가 확인해야 할 현재 제품의 기능 범위를 나타낸다. 상용화 전에는 각 인터페이스에 요청·응답 스키마, 권한, 계좌 격리, 오류 코드, 호출 제한 정책을 문서화해야 한다.

### 13.1 인증

- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/auth/session`
- `GET /api/auth/social/:provider/start`
- `GET /api/auth/:provider/callback`
- 회원가입 검증, SMS 요청·확인, 가입

### 13.2 시장·뉴스

- `GET /api/health`
- `GET /api/market/summary`
- `GET /api/market/chart/:id`
- `GET /api/news`
- `GET /api/groups`

### 13.3 계좌·관심종목

- `GET /api/trade/providers`
- `GET /api/trade/acct`
- `POST /api/trade/acct`
- `DELETE /api/trade/acct/:id`
- `POST /api/trade/acct/:id/main`
- `GET /api/trade/watch`
- `POST /api/trade/watch`
- `DELETE /api/trade/watch/:id`
- `POST /api/trade/watch/rows`

### 13.4 종목·시세

- `GET /api/trade/stock/search`
- `POST /api/trade/stock/rows`
- `GET /api/trade/quote/:code`
- `GET /api/trade/buyable/:code`
- `GET /api/trade/chart/:code`

### 13.5 전략·신호

- `GET /api/trade/strat`
- `POST /api/trade/strat`
- `POST /api/trade/strat/:id/enabled`
- `DELETE /api/trade/strat/:id`
- `POST /api/trade/strat/:id/test`
- `GET /api/trade/sig`
- `POST /api/trade/sig/:id/ok`
- `POST /api/trade/sig/:id/skip`

### 13.6 주문·체결·잔고

- `POST /api/trade/order`
- `GET /api/trade/orders`
- `GET /api/trade/executions`
- `GET /api/trade/order-events`
- `POST /api/trade/orders/:id/cancel`
- `POST /api/trade/orders/:id/revise`
- `POST /api/trade/orders/:id/manage`
- `GET /api/trade/balance`
- `POST /api/trade/balance/refresh`
- `GET /api/trade/holding/:code`
- `GET /api/trade/pos`

### 13.7 포지션·운영 제어

- `POST /api/trade/pos/strategy`
- `POST /api/trade/pos/trail-arm`
- `POST /api/trade/pos/stop-pause`
- `POST /api/trade/pos/auto-sell-pause`
- `GET /api/trade/ctrl`
- `POST /api/trade/ctrl`
- `POST /api/trade/calc`
- `GET /ws`

---

## 14. 단계별 개발 로드맵

개발 순서는 사용자가 눈으로 확인하기 쉬운 기능보다 실제 거래 위험이 큰 영역을 먼저 해결하는 방식으로 구성한다. 각 단계는 단순 개발 완료가 아니라 측정 가능한 완료 기준을 통과해야 다음 단계로 넘어간다.

### Phase 0. 거래 안전화

첫 단계에서는 새로운 감지 기능을 늘리지 않고 주문과 계좌 상태를 안정화한다. 동일한 신호가 두 번 주문되지 않도록 멱등성을 적용하고, 증권사 원장과 내부 상태를 주기적으로 맞추는 Reconciliation Worker를 구축한다. 부분 체결과 정정·취소도 하나의 명확한 상태 머신으로 관리한다.

- 주문 멱등키
- 계좌별 Trading Leader
- 증권사 원장 Reconciliation Worker
- 부분 체결·정정·취소 상태 머신
- 실시간 시세 stale guard
- 신규 매수 Kill Switch
- DB migration 체계

완료 기준:

- 모의투자 1,000건 주문에서 중복 주문 0건
- 주문·체결·잔고 수량 불일치 0건
- 재시작 후 진행 주문 복구

### Phase 1. 감지 신뢰성

주문 경로가 안정된 이후에는 급등 감지가 실제 시장 데이터를 빠짐없이 받고 있는지 검증한다. 어떤 종목이 구독에서 누락됐는지 자동으로 확인하고, 모든 신호에 사용된 원본 틱과 조건 평가 결과를 저장한다. 이 단계가 완료되어야 “차트에서는 올랐는데 왜 감지하지 못했는가”라는 질문에 데이터로 답할 수 있다.

- 구독 관리자
- 구독 누락 경보
- 원본 틱 저장
- 조건별 통과·실패 로그
- 틱 리플레이
- Shadow Mode
- API 급등 감지 검증 앱 1차

완료 기준:

- 저장된 틱으로 동일 신호 재현
- 구독 누락을 5초 이내 감지
- 전략 조건 결과를 화면에서 설명 가능

### Phase 2. 전략 검증

감지 결과를 재현할 수 있게 되면 전략의 성능을 검증한다. 전략 파라미터 변경 이력을 버전으로 저장하고, 수수료·세금·슬리피지를 포함한 동일 계산 기준으로 결과를 비교한다. 최소 20거래일 동안 Shadow Mode를 운영하여 우연한 단기 성과가 아닌지 확인한다.

- 전략 버전 관리
- 수수료·세금·슬리피지 모델
- 전략별 분석
- 장 시작 5분 템플릿
- 위험 한도 프리셋

완료 기준:

- 최소 20거래일 Shadow Mode
- 전략별 승률·손익비·낙폭 산출
- 실전 활성화 승인 기준 수립

### Phase 3. 고급 기능

고급 미체결 관리와 다중 계좌 구조를 안정화한 다음에 물타기와 불타기 같은 추가 주문 기능을 도입한다. 이 단계에서도 기능을 한 번에 실전에 공개하지 않고 Shadow Mode, 모의투자, 제한된 실전 계좌 순서로 활성화한다.

- 물타기
- 불타기
- 고급 미체결 주문 관리
- 다중 계좌
- 다중 증권사
- 원격 알림

### Phase 4. 로컬 라이선스 제품

마지막 단계에서는 서버 코드의 저장소와 이벤트 의존성을 분리하고 Windows에서 독립적으로 실행되는 거래 서비스를 만든다. 설치, 라이선스 발급, 자동 업데이트, 장애 복구까지 포함해야 실제 판매 가능한 설치형 제품이 된다.

- 저장소·이벤트 인터페이스 분리
- Windows Service
- SQLite·DPAPI
- 라이선스 발급 서버
- 서명 업데이트
- 설치 프로그램
- 오프라인 유예기간

---

## 15. 품질 보증 계획

자동매매 제품의 테스트는 화면이 렌더링되는지 확인하는 수준으로 충분하지 않다. 가격 계산과 수량 계산 같은 작은 함수부터 부분 체결·정정·취소가 얽힌 상태 머신, 실제 증권사 모의투자까지 단계별 검증이 필요하다. 특히 한 번 수정한 장애가 다시 발생하지 않도록 운영 사고마다 재현 테스트를 추가한다.

### 15.1 단위 테스트

단위 테스트는 외부 API 없이도 항상 같은 결과가 나와야 하는 계산을 대상으로 한다. 호가 단위와 반올림, 상승률, 체결강도, 주문 수량, 수수료처럼 작은 오차가 실제 주문 가격과 손익을 바꿀 수 있는 항목을 우선한다.

- 가격 단위 계산
- 상승률
- 거래대금·거래량 집계
- 체결강도
- 매수 비율
- 순매수량
- 거래대금 가속도
- 익절·손절·고점 추적
- 주문 수량
- 수수료·세금

### 15.2 상태 머신 테스트

상태 머신 테스트는 주문이 정상적으로 전량 체결되는 경우뿐 아니라 응답 순서가 뒤바뀌거나 프로세스가 중간에 재시작되는 경우까지 포함한다. 각 경로에서 최종 체결 수량과 잔여 수량의 합이 원주문 수량과 일치해야 한다.

- queued → sent → partial → filled
- sent → cancel_pending → cancelled
- partial → revise_pending → partial/filled
- sent → rejected
- tracking_error → 원장 복구
- 프로세스 재시작 중 체결

### 15.3 통합 테스트

통합 테스트에서는 MySQL, Redis, WebSocket, KIS 모의투자를 실제 운영과 유사하게 연결한다. 네트워크 단절, 호출 제한, PM2 재시작을 의도적으로 발생시킨 뒤 주문 상태가 복구되고 다른 사용자나 계좌 데이터가 섞이지 않는지 확인한다.

- MySQL·Redis·WebSocket·KIS 모의투자
- 다중 사용자·다중 계좌 격리
- API rate limit
- 네트워크 단절과 복구
- PM2 재시작

### 15.4 실전 전 승인 조건

실전 자동매매는 개발자가 기능 구현을 완료했다는 이유만으로 활성화하지 않는다. P0 버그가 없고, 모의투자와 Shadow Mode에서 정해진 기간을 통과했으며, 운영자가 Kill Switch와 장애 복구 절차를 실제로 수행할 수 있을 때만 승인한다.

- P0 버그 0건
- P1 버그의 우회책과 경보 마련
- 모의투자 20거래일
- Shadow Mode 20거래일
- 최대 손실과 Kill Switch 검증
- 운영자가 장애 복구 절차를 수행할 수 있음

---

## 16. 운영 KPI

운영 KPI는 수익률만으로 구성하면 안 된다. 주문이 누락되거나 데이터가 오래된 상태에서 우연히 수익이 발생할 수도 있기 때문이다. 먼저 시스템이 정확하고 안정적으로 동작하는지를 측정하고, 그 위에서 거래 품질과 전략 성과를 평가한다.

### 안정성

안정성 지표는 서비스가 켜져 있었는지뿐 아니라 필요한 데이터가 제시간에 도착했는지를 보여준다. WebSocket 연결 유지율, 틱 누락률, 주문·잔고 불일치율과 이벤트 처리 지연을 함께 측정한다.

- API 가용률
- WebSocket 연결 유지율
- 틱 누락률
- 구독 성공률
- 주문 상태 불일치율
- 잔고 불일치율
- 프로세스 메모리
- 이벤트 처리 지연

### 거래

거래 지표는 신호가 실제 주문과 체결로 이어지는 과정의 품질을 평가한다. 주문 거부와 미체결이 많다면 전략 성과 이전에 주문 방식과 호출 제한부터 조정해야 한다.

- 감지 신호 수
- 승인·자동 주문 비율
- 주문 거부율
- 부분 체결률
- 미체결률
- 평균 체결 지연
- 평균 슬리피지
- 정정·취소 성공률

### 전략

전략 지표는 동일한 수수료·세금·슬리피지 기준을 적용한 뒤 산출한다. 단순 승률보다 평균 수익과 평균 손실의 비율, 최대 낙폭, 감지 후 유리·불리 움직임을 함께 봐야 실제 운용 가능성을 판단할 수 있다.

- 전략별 매수·매도 금액
- 승률
- 평균 수익·손실
- 손익비
- Profit Factor
- 최대 낙폭
- 감지 후 최대 유리·불리 움직임

---

## 17. 경영진 결정 필요 사항

기술팀만의 판단으로 결정하기 어려운 제품 범위와 위험 수용 수준은 경영진 합의가 필요하다. 특히 실전 자동매매 공개 시점과 로컬 설치형 제품의 우선순위는 개발 구조와 운영 비용을 크게 바꾸므로 초기에 방향을 정해야 한다.

1. 당분간 실전 자동매매를 제한하고 안정화·Shadow Mode를 우선할 것인지
2. 웹 SaaS와 로컬 라이선스 제품 중 1차 상용화 우선순위
3. 지원할 증권사와 거래 세션 범위
4. 사용자별 최대 감시 종목·전략·계좌 수
5. 라이선스 등급과 오프라인 허용 기간
6. 틱 데이터 보관 기간과 저장 비용
7. 고객 지원 시 원격 로그 제공을 선택 사항으로 받을지
8. 물타기 기능을 Pro 이상으로 제한할지
9. 실제 주문 기능 출시 전 필요한 법률·약관·면책 검토 범위

---

## 18. 최종 제안

나비트레이딩은 기능 수가 부족한 제품이 아니라, 이미 많은 기능이 연결된 대신 **실시간 거래 시스템의 정합성과 장애 복구를 제품 수준으로 끌어올려야 하는 단계**다.

현재까지의 개발과 운영 테스트는 실패가 아니라 상용화 전에 필요한 문제를 실제 환경에서 발견한 과정이다. 계좌 연결, 실시간 감지, 주문, 체결 추적, 자동 청산까지 핵심 흐름이 이미 존재하기 때문에 이제는 기능의 개수보다 그 흐름이 끊기지 않는지를 증명하는 일이 중요하다.

신규 기능을 계속 추가하면 화면은 풍부해지지만 주문·체결·잔고의 핵심 신뢰성이 더 복잡해질 수 있다. 따라서 다음 개발 순서는 반드시 아래와 같아야 한다.

1. 주문·체결·잔고 정합성
2. 실시간 구독 안정성
3. 판단 근거 저장과 리플레이
4. Shadow Mode 성능 검증
5. 위험 관리 강화
6. 물타기 등 고급 기능
7. 로컬 라이선스 제품

상용화 판단은 “주문이 들어갔다”가 아니라 다음 질문에 모두 답할 수 있을 때 내려야 한다.

- 왜 이 종목을 감지했는가?
- 어떤 값이 조건을 통과했는가?
- 왜 이 가격과 수량으로 주문했는가?
- 주문은 현재 어떤 상태인가?
- 증권사 원장과 내부 상태가 같은가?
- 문제가 생기면 신규 매수를 즉시 중지하고 보유 포지션을 안전하게 관리할 수 있는가?

이 기준을 충족하면 현재 프로토타입은 웹 서비스, 로컬 라이선스 제품, 내부 API 검증 도구로 확장할 수 있다.

단기적으로는 거래 안전화와 감지 재현성을 확보하고, 중기적으로는 Shadow Mode를 통해 전략 성능을 객관적으로 검증한다. 이후에만 물타기와 같은 고급 기능을 추가하고 로컬 라이선스 제품으로 전환한다. 이 순서를 지키면 지금까지 축적한 코드를 버리지 않으면서도 실거래 프로토타입을 판매와 장기 운영이 가능한 제품으로 발전시킬 수 있다.
