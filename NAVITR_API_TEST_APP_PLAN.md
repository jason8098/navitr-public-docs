# 나비트레이딩 API 급등 감지 검증 앱 기획서

> 문서 버전: 1.0  
> 작성 기준일: 2026-07-30  
> 제품 성격: 개발·QA·장애 분석용 독립 테스트 애플리케이션  
> 문서 목적: 증권사 API와 급등 감지 엔진의 기능을 운영 서비스와 분리해 검증하기 위한 개발 범위 정의

---

## 1. 기획 배경

나비트레이딩 운영 화면은 실제 사용자가 시장을 감시하고 주문을 관리하는 서비스지만, API 급등 감지 검증 앱은 고객 대상 제품이 아니라 내부 개발·QA·장애 분석을 위한 도구다. 따라서 원본 API 요청과 응답, 종목별 구독 상태, 실시간 틱 수신 과정, 전략 조건별 입력값과 계산값, 조건 통과·탈락 사유, 주문 처리 단계, 오류 내용 등 문제 분석에 필요한 정보를 가리지 않고 모두 제공한다.

운영 과정에서 신호가 발생하지 않거나 주문이 거부됐을 때도 원인이 한 가지로 정리되지 않았다. 실제 틱이 들어오지 않은 경우, 전략 조건에서 탈락한 경우, 증권사 호출 한도를 초과한 경우, 주문 상태가 내부 DB와 증권사 원장에서 달라진 경우가 화면에서는 비슷한 오류처럼 보일 수 있었다.

따라서 운영 앱과 분리된 **API 급등 감지 검증 앱**을 개발한다. 이 앱은 수익을 위한 매매 화면이 아니라 증권사 API의 모든 주요 기능을 직접 실행하고, 입력부터 최종 결과까지의 처리 과정을 재현하는 내부 품질 도구다.

---

## 2. 개발 목적

검증 앱의 목적은 API 연결 여부를 확인하는 데 있지 않다. **마켓 중심 종목에서 실제 자금이 몰리는 순간을 틱 단위로 기록하고, 어떤 조건 조합이 급등 초입을 가장 빠르고 정확하게 포착했는지 검증하는 것**이 핵심이다.

이를 위해 마켓 중심 종목의 원본 체결을 빠짐없이 수집하고, 단일 체결대금, 구간 거래대금, 거래량, 체결강도, 매수 체결 비율, 순매수 거래량과 가격 변화가 어떤 순서로 발생했는지 하나의 시간축으로 재구성한다. 신호가 발생한 건만 남기는 것이 아니라 조건을 통과하지 못한 모든 구간도 함께 저장해, 임계값을 바꿨을 때 결과가 어떻게 달라지는지 다시 계산할 수 있어야 한다.

특히 장 시작 직후 5분처럼 수급이 급격하게 변하는 구간을 별도로 분석한다. 단순히 가격만 오른 종목과 실제 매수 자금이 연속적으로 유입된 종목을 구분하고, 일회성 대량 체결, 허수성 급등, 이미 상승이 끝난 종목을 걸러낼 수 있는 조건을 찾는다.

각 신호에는 발생 당시의 원본 틱과 계산값뿐 아니라 이후 10초, 30초, 1분, 3분, 5분의 수익률, 최대 상승폭과 최대 하락폭을 연결한다. 이를 통해 감지 속도, 적중률, 기대수익, 손실 위험을 조건 조합별로 비교하고, 사람이 직접 분석하거나 추후 AI 모델의 학습·검증 데이터로 사용할 수 있는 형태로 축적한다.

1차 목표는 특정 조건을 한 번 실행해 보는 화면이 아니라, **실시간 수급 기록, 조건 조정, 과거 틱 재생, 신호 성과 비교를 하나의 테스트 세션에서 반복할 수 있는 급등 감지 연구 환경**을 만드는 것이다.

---

## 3. 제품 범위와 원칙

검증 앱은 운영 앱과 UI 및 실행 목적을 분리하지만, 실제 검증 대상인 API 클라이언트와 전략 계산 모듈은 운영 코드와 동일한 것을 사용한다. 테스트 앱만의 별도 계산 로직을 만들면 운영 결과와 테스트 결과가 달라질 수 있기 때문이다.

주문 Sandbox의 기본 계좌는 모의투자로 제한한다. 실전 계좌 주문은 환경 설정, 별도 잠금 해제, 2단계 사용자 확인이 모두 충족된 경우에만 허용한다. 조회 기능은 실제 계좌에서도 사용할 수 있지만 인증정보와 원본 응답에 포함된 민감정보는 항상 마스킹한다.

모든 테스트는 세션 단위로 기록한다. 한 테스트에서 발생한 요청, 응답, 틱, 전략 판정, 주문 상태 변화, 처리 시간과 오류를 같은 세션 ID로 묶어야 장애 보고서와 회귀 테스트 자료로 재사용할 수 있다.

---

## 4. 기본 사용 흐름

검증 앱의 기본 사용 흐름은 다음과 같다.

`연결 확인 → 계좌 선택 → 종목 선택 → 원본 틱 확인 → 급등 조건 실행 → 신호 검증 → 모의 주문 → 부분 체결·정정·취소 → 증권사 원장 대조 → 결과 저장`

사용자는 먼저 API 서버, Redis, MySQL, 증권사 REST 인증과 WebSocket 연결 상태를 확인한다. 이후 종목을 선택해 실제 틱이 수신되는지 보고, 원하는 급등 조건을 현재 데이터에 적용한다. 신호가 생성되면 모의 주문으로 주문·체결 흐름을 검증하고, 마지막으로 내부 상태와 증권사 원장을 대조한다.

---

## 5. 화면 구성

### 5.1 연결 상태

연결 상태 화면은 테스트를 시작하기 전에 필요한 모든 의존 서비스가 정상인지 확인하는 첫 화면이다. 단순 연결 여부뿐 아니라 마지막 정상 응답 시각과 지연 시간, 구독 승인 여부를 함께 보여준다.

- API 서버 상태
- MySQL·Redis 상태
- 증권사 REST 인증 상태
- 증권사 WebSocket 상태
- 접속 계좌와 모의·실전 구분
- 마지막 heartbeat
- 마지막 틱 시각
- REST 평균·최대 응답 시간
- 구독 요청 수, 승인 수, 실패 수
- API 호출 제한 사용량
- 최근 연결·재연결 오류

### 5.2 종목·시세 테스트

종목·시세 화면에서는 종목을 코드나 이름으로 검색하고, 수신된 원본 틱과 서버가 계산한 값을 동시에 확인한다. 화면에 표시된 현재가와 급등 엔진이 사용한 현재가가 같은지도 비교할 수 있어야 한다.

- 종목 코드·이름 검색
- 현재가와 호가 단위
- 틱 스트림
- 틱 거래대금
- 거래량
- 당일 누적 거래대금
- 체결 방향
- 체결강도
- 매수 체결 비율
- 순매수량
- 분봉·일봉·틱 차트
- 마지막 수신 시각과 데이터 지연
- 원본 요청·응답 보기

### 5.3 급등 조건 실험기

급등 조건 실험기는 운영 전략의 모든 파라미터를 입력하고 현재 틱 또는 저장된 틱에 즉시 적용하는 화면이다.

- 감지 구간
- 구간 상승률
- 거래대금
- 거래량
- 총 거래대금
- 최소 체결 수
- 체결강도
- 매수 체결 비율
- 순매수 거래량
- 구간 거래대금
- 구간 거래량
- 거래대금 가속도
- 감시 범위
- 거래 세션

각 조건은 다음 형식으로 표시한다.

`조건명 / 기준값 / 실제값 / 통과 여부 / 계산 시각 / 사용한 원본 틱`

전체 조건을 충족하지 못한 경우에는 첫 번째 실패 조건만 보여주지 않고, 모든 조건의 통과·실패 결과를 동시에 제공한다. 이를 통해 조건 하나를 수정했을 때 다른 조건 때문에 다시 실패하는 상황을 줄인다.

#### 마켓 중심 종목 실시간 분석 로그

검증 앱은 현재 마켓 중심 화면에 포함된 모든 종목을 하나의 감시 유니버스로 관리한다. 화면에 표시된 종목뿐 아니라 장중 그룹 변경으로 새로 편입되거나 제외된 종목의 이력도 함께 기록한다.

신호가 발생한 순간만 저장해서는 감지 조건이 왜 둔감했는지 분석할 수 없다. 따라서 마켓 중심 종목에서 수신한 모든 정상 틱과 조건 판정 결과를 기록하되, UI 메모리에 계속 쌓아두지 않고 서버에서 날짜별 로그 파일과 분석용 저장소로 순차 기록한다.

각 틱에는 최소한 다음 정보를 포함한다.

- 증권사 체결 시각과 서버 도착 시각의 차이 및 수신 지연
- 종목 코드, 종목명, 시장, 현재 소속 그룹
- 체결가, 직전 체결가, 가격 변화량과 변화율
- 단일 체결 수량과 단일 체결 거래대금
- 당일 누적 거래량과 누적 거래대금
- 매수·매도 체결 방향
- 체결강도, 매수 체결 비율, 순매수량
- 연속 매수·매도 체결 수
- 감지 구간의 틱 수, 거래량, 거래대금과 상승률
- 직전 구간 대비 거래량·거래대금 증가 배수
- 적용한 전략과 전략 버전
- 각 조건의 기준값, 실제값, 통과·실패 여부
- 최종 신호 생성 여부와 탈락 사유
- WebSocket 구독 상태, 수신 순번, 중복·누락 여부

로그는 사람이 직접 확인할 수 있는 타임라인과 CSV·JSON 형태로 제공한다. 같은 데이터를 AI 분석에 사용할 때는 종목과 시각별 특징값을 정규화한 데이터셋도 별도로 생성한다. 향후 수익률을 정답처럼 미리 섞지 않고, 신호 발생 후 10초·30초·1분·3분·5분의 가격 변화, 최대 상승폭, 최대 하락폭을 결과 라벨로 나중에 결합한다.

### 5.4 신호 리플레이

신호 리플레이 화면은 저장된 틱을 원래 순서대로 다시 실행해 과거 신호를 재현한다.

- 저장된 날짜·종목 선택
- 1배, 10배, 최대 속도 재생
- 특정 시각 정지와 한 틱씩 이동
- 전략 파라미터 변경 후 재평가
- 운영 당시 결과와 현재 코드 결과 비교
- 신호 누락 원인 비교
- 결과 JSON·CSV 내보내기

### 5.5 주문 Sandbox

주문 Sandbox는 매수·매도부터 부분 체결, 정정, 취소, 원장 대조까지 주문의 전체 수명주기를 검증한다. 기본값은 모의투자이며 실전 주문은 별도 잠금 해제와 2단계 확인을 요구한다.

- 매수·매도
- 시장가·지정가·지정가 후 시장가
- 수량·금액·계좌 비율
- 매수 가능 금액·수량
- 호가 단위 검증
- 주문 접수 요청과 원본 응답
- 부분 체결
- 미체결
- 정정
- 취소
- 원주문·정정주문·취소주문 연결 관계
- 주문별 상태 변경 타임라인
- 증권사 원장 재조회

### 5.6 장애 주입

장애 주입 화면은 운영 중 발생 가능한 상황을 의도적으로 만들고 시스템이 안전하게 복구되는지 확인한다.

- WebSocket 강제 종료
- REST timeout
- Redis 중단
- DB 지연
- 중복 체결 통지
- 체결 통지 누락
- API rate limit
- 프로세스 재시작
- 오래된 시세
- 응답 순서 뒤바꿈

장애 주입은 운영 실계좌에서는 사용할 수 없도록 환경과 권한을 분리한다.

### 5.7 자동 검증 결과

자동 검증 결과 화면은 테스트 세션의 성공·실패를 항목별로 보여주고, 실패한 경우 관련 요청과 로그 위치를 연결한다.

- 요청·응답 스키마 검증
- 민감정보 마스킹 여부
- 주문 멱등성
- 계좌 데이터 격리
- 신호와 전략 연결
- 주문과 체결 수량 보존
- 포지션 수량과 증권사 잔고 일치
- 손익 계산과 증권사 원장 비교
- WebSocket 누락 후 복구 여부
- 테스트 처리 시간과 병목 구간

---

## 6. 핵심 검증 기능

이 앱은 로그인이나 일반적인 계좌 관리 기능을 확인하는 도구가 아니다. 핵심은 **마켓 중심 종목에서 실제 수급이 몰리는 순간을 빠짐없이 기록하고, 급등 조건을 반복 조정하면서 어떤 조건 조합이 유효했는지 검증하는 것**이다.

| 영역 | 핵심 기능 |
|---|---|
| 감시 유니버스 | 마켓 중심 종목 목록과 그룹 편입·제외 시각을 기록하고, 현재 감시 중인 종목과 WebSocket 구독 상태를 실시간으로 대조 |
| 원본 틱 수집 | 체결 시각, 수신 시각, 체결가, 체결 수량, 체결 방향, 단일 체결 거래대금, 누적 거래량·거래대금, 수신 순번을 틱 단위로 저장 |
| 수급 지표 | 체결강도, 매수 체결 비율, 순매수량, 연속 매수 체결, 구간 매수·매도 수량을 계산하고 원본값과 함께 기록 |
| 급등 구간 계산 | 설정한 시간 또는 틱 구간의 상승률, 체결 수, 거래량, 거래대금, 직전 구간 대비 증가 배수와 가속도를 매 틱 재계산 |
| 조건 판정 | 전략별 모든 조건의 기준값·실제값·통과 여부를 저장하고, 신호 미발생 시에도 전체 탈락 사유를 남김 |
| 조건 조정 | 하나의 저장 구간에 여러 조건 조합을 일괄 적용해 감지 건수, 중복 신호, 누락 신호, 오탐 비율을 비교 |
| 신호 결과 추적 | 신호 후 10초·30초·1분·3분·5분 수익률과 최대 상승폭·최대 하락폭을 계산해 감지 품질을 평가 |
| 리플레이 | 저장된 틱을 원래 순서, 배속 또는 한 틱 단위로 재생하고 운영 당시 결과와 변경된 전략 결과를 비교 |
| 데이터 품질 | WebSocket 구독 실패, 틱 누락, 중복, 순서 역전, 오래된 시세, 필드 누락과 수신 지연을 종목별로 표시 |
| 분석 화면 | 종목별 가격·거래대금·체결강도 타임라인, 급증 구간, 전략 판정 시점과 탈락 조건을 한 화면에서 비교 |
| 분석 로그 | 마켓 중심 종목의 원본 틱, 파생 지표, 전략 판정과 신호 결과를 날짜·종목·전략별로 검색하고 CSV·JSON으로 내보냄 |
| AI 분석 데이터 | 원본 틱과 파생 특징값, 전략 판정, 신호 이후 성과 라벨을 분리된 스키마로 생성해 모델 학습과 오프라인 분석에 사용 |
| 주문 연계 검증 | 신호가 실제 주문으로 이어진 시각, 주문 가격·수량, 접수·거부·부분 체결·정정·취소 결과와 전체 지연 시간을 신호에 연결 |

---

## 7. 필수 테스트 시나리오

1. 마켓 중심 종목으로 편입된 시점부터 제외된 시점까지 모든 정상 틱이 순서대로 기록된다.
2. 단일 체결 거래대금과 당일 누적 거래대금이 섞이지 않고 원본 API 값과 일치한다.
3. 체결강도, 매수 체결 비율과 순매수량이 같은 시각의 원본 체결 내역으로 재계산된다.
4. 전략에 포함된 모든 조건이 매 틱 평가되고 기준값·실제값·판정 결과가 함께 저장된다.
5. 신호가 발생하지 않은 구간도 탈락 조건을 조회할 수 있다.
6. 동일한 저장 틱과 동일한 전략 버전으로 리플레이하면 같은 신호 결과가 나온다.
7. 조건값을 변경한 뒤 과거 구간을 재평가하면 기존 결과와 변경 결과를 나란히 비교할 수 있다.
8. 짧은 시간에 거래대금과 체결강도가 동시에 급증한 구간이 타임라인에 표시된다.
9. 가격만 상승하고 매수 수급이 약한 구간과 실제 자금 유입이 동반된 구간을 구분할 수 있다.
10. 신호 후 10초·30초·1분·3분·5분 성과와 최대 상승폭·최대 하락폭이 자동 계산된다.
11. WebSocket 틱이 누락되거나 순서가 바뀌면 해당 종목과 누락 범위가 명시된다.
12. 구독 한도를 초과하거나 재연결에 실패하면 감지 실패로 숨기지 않고 데이터 수신 실패로 구분한다.
13. 서버 재시작 후에도 이전 로그와 전략 버전을 이용해 같은 구간을 다시 분석할 수 있다.
14. 장시간 수집해도 원본 틱이 브라우저 메모리에 누적되지 않고 서버 저장소로 순차 기록된다.
15. 분석 데이터 내보내기 결과만으로 사람이 수동 분석하거나 AI 학습 데이터셋을 생성할 수 있다.
16. 신호가 주문으로 이어지지 않은 경우 조건 탈락, 위험 제한, API 거부와 시스템 오류를 구분해 표시한다.
17. 부분 체결·정정·취소가 발생하면 신호부터 최종 주문 상태까지 하나의 타임라인으로 연결된다.

---

## 8. 테스트 결과 산출물

각 테스트 세션은 다음 산출물을 생성한다.

- 마켓 중심 종목의 편입·제외 이력
- 체결 시각과 서버 수신 시각이 포함된 원본 틱
- 단일 체결 거래대금과 누적 거래대금을 구분한 거래 기록
- 체결강도, 매수 체결 비율, 순매수량 등 파생 수급 지표
- 전략 버전과 조건별 기준값·실제값·통과 여부
- 신호 생성 또는 탈락 사유
- 신호 후 구간별 수익률, 최대 상승폭과 최대 하락폭
- WebSocket 구독·누락·중복·지연 상태
- 조건 조합별 감지 건수, 오탐과 누락 비교표
- 신호부터 주문·체결·정정·취소까지의 처리 타임라인
- 사람이 직접 분석할 수 있는 CSV·JSON 로그
- AI 학습에 사용할 특징값과 결과 라벨 데이터셋
- 동일 상황 재현에 필요한 틱 리플레이 데이터

결과 화면에서는 특정 날짜, 시간, 종목, 전략을 기준으로 데이터를 검색할 수 있어야 한다. 원본 틱, 파생 지표와 전략 판정 결과를 서로 연결된 형태로 내보내 사람이 직접 분석하거나 AI 분석 파이프라인에 바로 전달할 수 있어야 한다.

---

## 9. 개발 단계

### 1단계: 연결·시세 진단

마켓 중심 종목의 구성과 WebSocket 실제 구독 상태를 대조하고, 모든 원본 틱을 날짜별로 저장하는 기능을 먼저 구현한다. 단일 체결 거래대금, 누적 거래대금과 체결강도가 원본 데이터와 일치하는지 검증해 급등 감지 누락의 원인이 데이터 수신인지 전략 조건인지 구분할 수 있는 수준을 목표로 한다.

### 2단계: 조건 실험·리플레이

모든 전략 파라미터를 매 틱 실행하고 조건별 실제값과 통과 여부를 표시한다. 저장된 틱을 재생해 과거 운영 결과를 동일하게 재현하고, 여러 조건 조합의 감지 성능을 한 번에 비교한다.

### 3단계: 성과 분석·AI 데이터 생성

각 신호 이후의 단기 수익률, 최대 상승폭과 최대 하락폭을 자동 계산한다. 사람이 직접 검토할 수 있는 분석표와 AI 학습에 사용할 특징값·결과 라벨 데이터셋을 생성한다.

### 4단계: 주문 연계·장애 재현

검증된 신호를 모의 주문과 연결해 접수, 부분 체결, 정정과 취소 과정을 확인한다. 네트워크 단절, 호출 제한, 이벤트 누락과 프로세스 재시작을 재현하고 운영 중 발견된 버그를 회귀 테스트 항목으로 계속 추가한다.

---

## 10. 완료 기준

API 급등 감지 검증 앱의 1차 버전은 다음 조건을 충족할 때 완료로 판단한다.

- 종목별 실제 구독 여부와 마지막 틱 시각을 확인할 수 있다.
- 마켓 중심 종목의 모든 정상 틱과 그룹 편입·제외 이력을 날짜별로 저장한다.
- 단일 체결 거래대금, 누적 거래대금과 체결강도를 원본 데이터와 구분해 기록한다.
- 급등 전략의 모든 조건에 대해 기준값·실제값·통과 여부를 표시한다.
- 신호가 발생하지 않은 틱에도 탈락한 모든 조건과 실제값이 남는다.
- 저장된 틱으로 동일 신호를 재현할 수 있다.
- 여러 조건 조합의 감지 결과와 신호 이후 성과를 비교할 수 있다.
- 수동 분석용 로그와 AI 학습용 특징값·결과 라벨 데이터를 내보낼 수 있다.
- 모의 주문의 접수·부분 체결·정정·취소 전 과정을 추적할 수 있다.
- 내부 주문·체결·잔고와 증권사 원장을 자동 비교한다.
- 운영 중 확인된 P0 주문 장애를 자동 회귀 테스트로 재현한다.

이 앱을 통해 운영 서비스에서 문제가 발생했을 때 추측으로 원인을 찾는 대신, 동일한 API와 데이터를 사용해 단계별로 재현하고 검증할 수 있는 체계를 마련한다.
